# Секреты — НИКОГДА не коммитим
.env

# Виртуальное окружение
.venv/
venv/

# Python-кеш
__pycache__/
*.py[cod]
*$py.class

# Логи бота (создаются на хосте при запуске)
data/bot.log

# Архив навданных Navigraph (большой, нужен только для локального импорта)
*.zip

# Локальные скрипты автозапуска Windows (на хостинге не нужны)
install_autostart.ps1
run_bot.bat
run_bot_hidden.vbs

# ВАЖНО: data/navdata.sqlite НЕ игнорируется — он нужен боту на хостинге.
